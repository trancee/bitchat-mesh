package com.bitchat.android.services.meshgraph

/**
 * Stub route planner for library builds (source routing disabled).
 */
object RoutePlanner {
    fun shortestPath(fromPeerID: String, toPeerID: String): List<String>? = null
}
